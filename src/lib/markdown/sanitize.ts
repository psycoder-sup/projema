/**
 * Markdown sanitization using isomorphic-dompurify.
 * Phase 3 implementation — Phase 0 stub only.
 */

// Phase 3: implement sanitizeMarkdown() using isomorphic-dompurify
export function sanitizeMarkdown(html: string): string {
  // Stub — Phase 3 wires isomorphic-dompurify
  return html;
}
