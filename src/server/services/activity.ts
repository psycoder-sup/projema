/**
 * Activity event service.
 * Phase 4 implementation — Phase 0 stub only.
 */

// Phase 4: implement writeActivityEvent() that inserts into activity_events
// inside a transaction and sends the corresponding PostHog event.
export {};
